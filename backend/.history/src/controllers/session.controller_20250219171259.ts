import { NextFunction, Request, Response } from "express";

export const login = async (
  req: Request<{}, {}, sessions["body"]>,
  res: Response,
  next: NextFunction
) => {
  try {
  } catch (error) {}
};

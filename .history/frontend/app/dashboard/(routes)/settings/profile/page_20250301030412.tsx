import React from 'react'
import dynamic from 'next/dynamic'
import Account from '@/components/settings/Account'
Account
const AccountProfile = dynamic(() => import("@/components/"))
const page = () => {
  return (

  )
}

export default page

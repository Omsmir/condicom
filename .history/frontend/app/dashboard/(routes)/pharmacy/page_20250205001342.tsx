import React, { Suspense } from "react";
import dynamic from "next/dynamic";

const PharmacyDynamic = dynami(())
const page = () => {
  return (
    <div>
      
    </div>
  )
}

export default page

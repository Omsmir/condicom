"use client"
import React from 'react'

interface StatsProps {
    StatsSection={breadCrumbString}
    StatsIcon={StatsIcon}
    data={data}
}

const Stats = () => {
  return (
    <div>
      
    </div>
  )
}

export default Stats

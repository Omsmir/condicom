import React from 'react'
import AreaChart from './Charts'

const Overview = async({id}:{id:string | undefined}) => {
  const user = await getuse
  return (
    <div >
      <AreaChart />
    </div>
  )
}

export default Overview
